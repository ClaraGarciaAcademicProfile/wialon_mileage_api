name: wialon_mileage
description: A handy Flutter app to check your mileage via the Wialon API
version: 1.0.0

environment:
  sdk: ">=3.7.2"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2
  provider: ^6.1.2
  google_fonts: ^6.2.1
  animate_do: ^4.2.0
  flutter_dotenv: ^5.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
